version: '3.8'

services:
  db:
    build: .
    image: pgproto-dev
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: pgproto
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - .:/workspace
    working_dir: /workspace
